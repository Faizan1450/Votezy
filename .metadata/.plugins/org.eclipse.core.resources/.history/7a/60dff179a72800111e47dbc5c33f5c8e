package in.scalive.votezy.service;

import org.springframework.stereotype.Service;

import in.scalive.votezy.entity.ElectionResult;
import in.scalive.votezy.repository.CandidateRepository;
import in.scalive.votezy.repository.ElectionResultRepository;
import in.scalive.votezy.repository.VoteRepository;

@Service
public class ElectionResultService {
	private ElectionResultRepository electionResultRespository;
	private CandidateRepository candidateRepository;
	private VoteRepository voteRepository;
	
	public ElectionResultService(ElectionResultRepository electionResultRespository, CandidateRepository candidateRepository, VoteRepository voteRepositor) {
		this.electionResultRespository = electionResultRespository;
		this.candidateRepository = candidateRepository;
		this.voteRepository = voteRepository;
	}
	
	public ElectionResult declareElectionResult(String electionName) {
		
	}
}
